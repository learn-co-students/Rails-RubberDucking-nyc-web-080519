<h1>These are some cool students:</h1>

<ul>
  <% @students.each do |student| %>
    <li> <%= link_to student.name, student_path(student) %> </li>
  <% end %>
</ul>
<br>
<p><%= link_to "See Ducks", ducks_path %></p>
<p><%= link_to "Create New Student", new_student_path %></p>