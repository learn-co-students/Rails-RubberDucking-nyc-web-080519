<h1>These are some cool students:</h1>

<ul>
  <% @students.each do |student| %>
    <li> <%= link_to student.name, student_path(student) %> </li>
  <% end %>
</ul>

<%= button_to "Add a new student", new_student_path, method: :get %>
<%= button_to "See list of ducks", ducks_path, method: :get %>