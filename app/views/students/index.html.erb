<h1>These are some cool students:</h1>

  <h2>
    <%= link_to 'Create a Student', new_student_url %>
  </h2>

<ul>
  <% @students.each do |student| %>
    <li> 
      <%= link_to student.name, student_url(student) %>
  <% end %>
</ul>
