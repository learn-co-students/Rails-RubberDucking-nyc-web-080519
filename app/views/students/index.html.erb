<h1>These are some cool students:</h1>

<ul>
  <% @students.each do |student| %>
    <li> Name: <%= link_to student.name, student_path(student) %>
    Module: <%= link_to student.mod, student_path(student) %> </li>
  <% end %>
</ul>
