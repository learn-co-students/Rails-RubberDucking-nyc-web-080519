<h1>These are some cool duckies:</h1>

<ul>
  <% @ducks.each do |duck| %>
    <li> <%= link_to duck.name, duck_path(duck) %> </li>
  <% end %>
</ul>

<%= button_to "Create a new duck", new_duck_path, method: :get %>
<%= button_to "List of students", students_path, method: :get %>
