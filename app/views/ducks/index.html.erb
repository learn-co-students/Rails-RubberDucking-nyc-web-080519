<h1>These are some cool duckys:</h1>

<ul>
  <% @ducks.each do |duck| %>
    <li> <%= link_to duck.name, duck_path(duck) %> 
    |
    <%= link_to("Delete", duck_path(duck), :method => "delete") %></li>
  <% end %>
</ul>
